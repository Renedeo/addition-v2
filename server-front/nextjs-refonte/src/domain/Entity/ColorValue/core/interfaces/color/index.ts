/**
 * Interfaces pour les couleurs et services associés.
 * 
 * Ce module centralise toutes les interfaces liées aux couleurs,
 * incluant les définitions de base et les services de palette.
 */

export type { 
  IColor, 
  IHEXColor, 
  IRGBColor, 
  IHSLColor 
} from './color.interface';

export * from './palette';