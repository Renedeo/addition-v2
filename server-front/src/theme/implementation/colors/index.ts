/**
 * Re-exports the colors configuration.
 * 
 * @example
 * ```ts
 * import { colors } from '@/theme/implementation/colors';
 * 
 * const primaryColor = colors.primary[500];
 * ```
 */
export { colors } from './colors';