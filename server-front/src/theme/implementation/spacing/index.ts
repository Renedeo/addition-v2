/**
 * Re-exports the spacing configuration.
 * 
 * @example
 * ```ts
 * import { spacing } from '@/theme/implementation/spacing';
 * 
 * const padding = spacing.spacing[4];
 * const radius = spacing.borderRadius.md;
 * ```
 */
export { spacing } from './spacing';