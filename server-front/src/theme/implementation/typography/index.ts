/**
 * Re-exports the typography configuration.
 * 
 * @example
 * ```ts
 * import { typography } from '@/theme/implementation/typography';
 * 
 * const [fontSize, { lineHeight }] = typography.fontSize.base;
 * const fontWeight = typography.fontWeight.medium;
 * ```
 */
export { typography } from './typography';